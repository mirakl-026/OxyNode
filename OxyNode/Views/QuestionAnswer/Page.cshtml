@model OxyNode.ViewModels.QAViewModel
@{
    ViewData["Title"] = "OxyNode | Qestion & Answers | Page "+Model.currentPageNumber;
}




<div>
    @if (Model.qaCount > 0)
    {
        <table>
            @for (int i = 0; i < Model.answers.Count; i++)
            {
                @*<p>
                        <div style="background-color:beige"><a asp-controller="QuestionAnswer" asp-action="ReadQA" asp-route-id="@Model.questions[i].AnswerId"> @Model.questions[i].FullName спрашивает </a>:
                        @Model.questions[i].questionText</div>

                        <div style="background-color:lightgreen">Ответ:
                        @Model.answers[i].answerText</div>
                    </p>*@

                <tr style="background-color:beige">
                    <td>
                        <div>@Model.questions[i].FullName,</div>
                        <div>@Model.questions[i].Address</div>
                    </td>
                    <td>
                        <div><a asp-controller="QuestionAnswer" asp-action="ReadQA" asp-route-answerId="@Model.questions[i].AnswerId" asp-route-questionId="@Model.questions[i].Id">Вопрос:</a></div>
                        <div>@Model.questions[i].questionText</div>
                    </td>
                </tr>

                <tr style="background-color:lightgreen">
                    <td>
                    </td>
                    <td>
                        <div>Ответ: </div>
                        <div>@Model.answers[i].answerText</div>
                    </td>
                </tr>
                <tr>
                    <td></td>
                    <td>
                        <br />
                    </td>
                </tr>

            }
        </table>
    }
</div>

<div>
    Страница: @Model.currentPageNumber
</div>
<div>
    Всего QA: @Model.qaCount
</div>