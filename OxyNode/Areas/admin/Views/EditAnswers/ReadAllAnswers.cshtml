@model OxyNode.ViewModels.AnswersViewModel

@{
    ViewData["Title"] = "Admin | QA | Read all answers";
}

<h3>QA: Все ответы</h3>
<hr />

<div>
    @foreach (var ans in Model.answers)
    {
        <p>
            <div><a asp-area="admin" asp-controller="EditAnswers" asp-action="ReadOneAnswer" asp-route-answerId="@ans.Id">Ответ: @ans.Id</a></div>
            <div>Содержание ответа:
                    @ans.answerText
            </div>
            @if (ans.QuestionId != null)
            {
                <div>Id вопроса: <a asp-area="admin" asp-controller="EditQuestions" asp-action="ReadOneQuestion" asp-route-questionId="@ans.QuestionId">@ans.QuestionId</a></div>
            }
            else
            {
                <div>Id вопроса: нет?</div>
            }
            <div>Флаг публикации на сайте: @ans.publishToSite</div>

            <div><a asp-area="admin" asp-controller="EditAnswers" asp-action="DeleteAnswer" asp-route-answerId="@ans.Id">Удалить ответ</a></div>
            <br />
            <br />
        </p>
    }

</div>

<hr />
<div>
    Страница ответов: @Model.currentPageNumber
</div>
<div>
    Всего ответов: @Model.answersCount
</div>

